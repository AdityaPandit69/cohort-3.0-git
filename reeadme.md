how to make git available on ur project

Step1. intialize ur repository
Step2. added stage
Step3. commit stage - checkpoint create krti h code k (always commit with msg so u can know)
if u want to check commits then write this in terminal {git log --oneline}
if we want to not to uplaod a file in git so make a file name {.gitignore}   and then inside it write that file name which u dont want to upload

how to clone someone's repo [copy http link which repo u want to clone and then open cmd {git clone and paste url}]
